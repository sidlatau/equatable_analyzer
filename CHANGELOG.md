## 1.1.0

- Upgrade to `analyzer` 14.x, `analyzer_plugin` 0.14.x and `analysis_server_plugin` 0.3.23, so the plugin resolves under the Dart 3.13 analysis server. Previously version solving failed, which disabled every plugin declared alongside it.
- Raise the minimum Dart SDK to 3.11.0, as required by `analyzer` 14.

## 1.0.0

- Initial version.
